from __future__ import annotations

from archive_backend.utils.small import registry

ViewContainerRegistry = registry("ViewContainers")
SerializerRegistry= registry(registry_name_for_debugging="Serializers")
